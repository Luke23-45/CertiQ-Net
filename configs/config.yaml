# ──────────────────────────────────────────────────────────────────────────────
# configs/config.yaml  — Hydra root config
#
# Change log:
#   - Removed `loss:` block (authoritative values live in cli/model/certiq_index.yaml)
#   - Removed `runner:` block (moved to configs/runner/default.yaml)
#   - Removed `sweep:` block (moved to configs/sweep/default.yaml)
#   - Kept `project:` and `env:` as primary knobs for every run
# ──────────────────────────────────────────────────────────────────────────────
defaults:
  - model: certiq_index
  - env: synthetic/family_a
  - adapter: queueing
  - trainer: default
  - logger: csv
  - progress: default
  - dataset: qgym/default
  - _self_

project:
  name: certiq-index
  seed: 42
  output_root: outputs
  experiment_name: null
  run_id: null
  resume_from_checkpoint: null
  log_dir: null
  save_checkpoints: true
