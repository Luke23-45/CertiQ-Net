# @package _global_
# ------------------------------------------------------------------------------
# configs/experiments/queueing/certiq_index_qgym.yaml
#
# CertiQ-Net training backed by pre-collected QGym states (static mode).
# Uses QGym-tuned PPO hyperparameters.
# ------------------------------------------------------------------------------
defaults:
  - /trainer: default
  - /model: certiq_index_qgym
  - /data: qgym

seed_everything: 42
datatype: qgym
