# CertiQ Dispatcher z3

This directory contains the z3 architecture formalization for CertiQ
Dispatcher.

Start with [formal_definition/README.md](./formal_definition/README.md).

The z3 work defines the architecture before implementation changes. The old
z2 package remains the current proof context for the CTMC backbone and gate
inheritance results.

